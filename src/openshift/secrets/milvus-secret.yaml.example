# Milvus credentials — apply via apply-secrets.sh
apiVersion: v1
kind: Secret
metadata:
  name: milvus-secret
type: Opaque
stringData:
  root-password: changeme
  MILVUS_ENDPOINT: "tcp://milvus-service:19530"
  MILVUS_TOKEN: ""
  MILVUS_CONSISTENCY_LEVEL: "Bounded"
